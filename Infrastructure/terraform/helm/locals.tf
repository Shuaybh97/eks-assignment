locals {
  charts = {
    traefik               = "traefik"
    cert_manager          = "cert-manager"
    external_dns          = "external-dns"
    kube-prometheus-stack = "kube-prometheus-stack"
    argocd                = "argocd"
  }
}
