# Kubernetes Manifests Standards

- Use YAML for all manifests
- Include resource limits and requests
- Use namespaces for logical separation
- Follow GitOps principles with ArgoCD
- Use proper RBAC configurations
- Always specify API versions explicitly
